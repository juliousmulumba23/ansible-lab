---
- hosts: devservers

  tasks:
    - name: create file under /tmp
      file:
        path: /tmp/hey.txt
        state: touch
      notify:
        - display a word

  handlers:
    - name: display a word
      shell: echo "file has been deleted" >> /tmp/my_file   
